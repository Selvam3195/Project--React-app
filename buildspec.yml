version: 0.2

phases:
  install:
    commands:
  build:
    commands:
      - kubectl apply -f deployment.yml --validation false # Apply your Deployment configuration
      - kubectl apply -f services.yml --validation false
