version: 0.0

Resources:
  - ViteApp:
      Type: Kubernetes::Manifest
      Properties:
        ClusterName: your-cluster-name
        Namespace: default
        TaskDefinition: k8s-manifests/deployment.yml
